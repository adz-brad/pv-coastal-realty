import { 
  properties,
  listings,
  property,
  regions, 
  zones
} from "./schema/index"

export const schema = {
  types: [
    properties,
    listings,
    property,
    regions,
    zones
  ],
}
